``` sh
gcloud functions deploy sample --gen2 --runtime=nodejs20 --trigger-http --allow-unauthenticated --region asia-northeast1
```

esbuild

``` sh
gcloud functions deploy sampleEsBuild --gen2 --runtime=nodejs20 --trigger-http --allow-unauthenticated --region asia-northeast1
```
